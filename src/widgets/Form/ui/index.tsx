import React from "react";
import { Input } from "@shared/ui/Input";

export const Form = () => {
  return (
    <form className="form">
      <Input placeholder="Введите ваше имя" />
    </form>
  );
};
